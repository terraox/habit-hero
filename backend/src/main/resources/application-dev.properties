# spring.datasource.url=jdbc:postgresql://localhost:5432/habithero
# spring.datasource.username=habithero
# spring.datasource.password=shipdock123
# spring.datasource.driver-class-name=org.postgresql.Driver

# H2 Configuration (for running without Docker)
spring.datasource.url=jdbc:h2:mem:habithero;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# JPA / Hibernate (optional but helpful)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Firebase (point to service account JSON that exists in project root)
FIREBASE_SERVICE_ACCOUNT_PATH=./firebase-service-account.json
FIREBASE_PROJECT_ID=habithero-540a4
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@habithero-540a4.iam.gserviceaccount.com